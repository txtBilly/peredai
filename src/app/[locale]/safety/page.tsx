import { isLocale } from '@/i18n/config';
import type { Locale } from '@/i18n/config';
import { notFound } from 'next/navigation';
import LegalPage from '@/components/LegalPage';

export default function SafetyPage({ params }: { params: { locale: string } }) {
  if (!isLocale(params.locale)) notFound();
  return (
    <LegalPage locale={params.locale as Locale} title="Безопасность">
      <p>
        Арендодатели подтверждают личность через Сбер ID, прежде чем разместить объявление. Тем, кто откликается,
        для начала общения достаточно указать имя и электронную почту и принять условия — оплата токена
        открывает доступ к чату.
      </p>
      <p>
        Ведите переписку внутри Ten2Ten. Никогда не сообщайте пароли, коды из СМС, данные банковских карт или
        доступ к Госуслугам — сервис их не запрашивает (кроме первичной регистрации через Сбер ID). Не переводите
        деньги и не вносите залог, пока не осмотрели квартиру и не договорились с человеком лично. Назначайте
        встречу в безопасном общественном месте.
      </p>
      <p>
        Если что-то кажется подозрительным, пожалуйтесь прямо из чата или объявления. Обращения рассматривает наша
        команда; сообщения в чатах не удаляются, поэтому мы видим, что произошло. Подтверждённые нарушения ведут к
        предупреждениям и блокировке.
      </p>
    </LegalPage>
  );
}
