diff --git a/src/app/[locale]/chats/[id]/ChatView.tsx b/src/app/[locale]/chats/[id]/ChatView.tsx
index 1e62734..a614671 100644
--- a/src/app/[locale]/chats/[id]/ChatView.tsx
+++ b/src/app/[locale]/chats/[id]/ChatView.tsx
@@ -334,7 +334,10 @@ export default function ChatView({ locale, id }: { locale: Locale; id: string })
               {c.terminate}
             </button>
           )}
-          <Link href={`/${locale}/chats/${id}/report`} className="text-sm text-muted hover:text-ink">
+          <Link
+            href={`/${locale}/chats/${id}/report`}
+            className="rounded-lg border border-black/15 px-3 py-1.5 text-sm text-muted transition hover:border-black/30 hover:text-ink"
+          >
             {c.report}
           </Link>
         </div>
diff --git a/src/app/[locale]/chats/[id]/report/ReportView.tsx b/src/app/[locale]/chats/[id]/report/ReportView.tsx
index eb80002..c472323 100644
--- a/src/app/[locale]/chats/[id]/report/ReportView.tsx
+++ b/src/app/[locale]/chats/[id]/report/ReportView.tsx
@@ -2,25 +2,14 @@
 
 import { useState } from 'react';
 import Link from 'next/link';
+import { getDictionary } from '@/i18n/config';
 import type { Locale } from '@/i18n/config';
 
 // Report a member/listing. The reason list depends on who's reporting: a renter
-// reporting a listing/lister vs. a lister reporting a seeker.
-const RENTER_REASONS: { value: string; label: string }[] = [
-  { value: 'unresponsive', label: 'Unresponsive' },
-  { value: 'unavailable', label: 'Apartment unavailable' },
-  { value: 'inaccurate', label: 'Inaccurate listing' },
-  { value: 'fraudulent', label: 'Fraudulent / scam' },
-  { value: 'something_else', label: 'Something else' },
-];
-
-const LISTER_REASONS: { value: string; label: string }[] = [
-  { value: 'unresponsive', label: 'Unresponsive' },
-  { value: 'not_serious', label: 'Not a serious renter' },
-  { value: 'inappropriate', label: 'Inappropriate or abusive messages' },
-  { value: 'fraudulent', label: 'Fraudulent / scam' },
-  { value: 'something_else', label: 'Something else' },
-];
+// reporting a listing/lister vs. a lister reporting a seeker. Labels come from
+// the `report.reasons` dictionary.
+const RENTER_REASON_KEYS = ['unresponsive', 'unavailable', 'inaccurate', 'fraudulent', 'something_else'];
+const LISTER_REASON_KEYS = ['unresponsive', 'not_serious', 'inappropriate', 'fraudulent', 'something_else'];
 
 export default function ReportView({
   locale,
@@ -33,7 +22,10 @@ export default function ReportView({
   listingId?: string;
   role?: 'lister' | 'seeker';
 }) {
-  const REASONS = role === 'lister' ? LISTER_REASONS : RENTER_REASONS;
+  const rp = getDictionary(locale).report;
+  const reasonLabels = rp.reasons as Record<string, string>;
+  const reasonKeys = role === 'lister' ? LISTER_REASON_KEYS : RENTER_REASON_KEYS;
+
   const [reason, setReason] = useState('');
   const [detail, setDetail] = useState('');
   const [phase, setPhase] = useState<'form' | 'submitting' | 'done' | 'error'>('form');
@@ -73,12 +65,10 @@ export default function ReportView({
     return (
       <main className="mx-auto flex min-h-[60vh] max-w-md flex-col items-center justify-center px-5 py-16 text-center">
         <p className="mb-2 text-sm uppercase tracking-wide text-cobalt">Peredai</p>
-        <h1 className="mb-2 font-display text-2xl text-ink">Report received</h1>
-        <p className="mb-8 text-sm text-muted">
-          Thanks — our team will review it. Messages are never deleted, so we can see exactly what happened.
-        </p>
+        <h1 className="mb-2 font-display text-2xl text-ink">{rp.doneTitle}</h1>
+        <p className="mb-8 text-sm text-muted">{rp.doneBody}</p>
         <Link href={backHref} className="text-sm text-cobalt hover:underline">
-          Back
+          {rp.back}
         </Link>
       </main>
     );
@@ -87,54 +77,48 @@ export default function ReportView({
   return (
     <main className="mx-auto max-w-md px-5 py-12">
       <p className="mb-2 text-sm uppercase tracking-wide text-cobalt">Peredai</p>
-      <h1 className="mb-2 font-display text-2xl text-ink">Report</h1>
-      <p className="mb-6 text-sm text-muted">
-        Peredai community is built on trust and safety. Tell us more. We got you.
-      </p>
+      <h1 className="mb-2 font-display text-2xl text-ink">{rp.title}</h1>
+      <p className="mb-6 text-sm text-muted">{rp.subtitle}</p>
 
       <form onSubmit={handleSubmit} className="flex flex-col gap-4">
         <fieldset className="flex flex-col gap-2">
-          {REASONS.map((r) => (
+          {reasonKeys.map((key) => (
             <label
-              key={r.value}
+              key={key}
               className={`flex cursor-pointer items-center gap-3 rounded-lg border px-4 py-3 text-sm transition ${
-                reason === r.value ? 'border-cobalt bg-cobalt/10 text-ink' : 'border-black/15 text-muted hover:border-black/30'
+                reason === key ? 'border-cobalt bg-cobalt/10 text-ink' : 'border-black/15 text-muted hover:border-black/30'
               }`}
             >
               <input
                 type="radio"
                 name="reason"
-                value={r.value}
-                checked={reason === r.value}
-                onChange={() => setReason(r.value)}
+                value={key}
+                checked={reason === key}
+                onChange={() => setReason(key)}
                 className="h-4 w-4 accent-cobalt"
               />
-              {r.label}
+              {reasonLabels[key]}
             </label>
           ))}
         </fieldset>
 
         <label className="flex flex-col gap-1 text-sm text-muted">
-          Tell us what happened (required)
+          {rp.detailLabel}
           <textarea
             required
             rows={4}
             value={detail}
             onChange={(e) => setDetail(e.target.value)}
-            placeholder="A few details help our team review this fairly."
+            placeholder={rp.detailPlaceholder}
             className="rounded-lg border border-black/15 bg-white px-3 py-2 text-ink placeholder:text-muted/60 outline-none focus-visible:ring-2 focus-visible:ring-cobalt"
           />
         </label>
 
-        {chatId && (
-          <p className="text-xs leading-relaxed text-muted">
-            By submitting, you consent to sharing this conversation with the Peredai team so we can review it fairly.
-          </p>
-        )}
+        {chatId && <p className="text-xs leading-relaxed text-muted">{rp.consent}</p>}
 
         {phase === 'error' && (
           <p role="alert" className="text-sm text-red-600">
-            Something went wrong ({error}). Please try again.
+            {rp.errorGeneric.replace('{error}', error)}
           </p>
         )}
 
@@ -144,10 +128,10 @@ export default function ReportView({
             disabled={!reason || !detail.trim() || phase === 'submitting'}
             className="rounded-lg bg-gradient-cobalt px-5 py-3 font-medium text-white transition hover:brightness-110 disabled:opacity-50"
           >
-            {phase === 'submitting' ? 'Submitting…' : 'Submit report'}
+            {phase === 'submitting' ? rp.submitting : rp.submit}
           </button>
           <Link href={backHref} className="text-sm text-muted hover:text-ink">
-            Cancel
+            {rp.cancel}
           </Link>
         </div>
       </form>
diff --git a/src/i18n/en.json b/src/i18n/en.json
index 4c5060d..2201b62 100644
--- a/src/i18n/en.json
+++ b/src/i18n/en.json
@@ -62,6 +62,29 @@
     "closedDidntWork": "This conversation was closed — didn't work out.",
     "closed": "This conversation is closed."
   },
+  "report": {
+    "title": "Report",
+    "subtitle": "Peredai community is built on trust and safety. Tell us more. We got you.",
+    "detailLabel": "Tell us what happened (required)",
+    "detailPlaceholder": "A few details help our team review this fairly.",
+    "consent": "By submitting, you consent to sharing this conversation with the Peredai team so we can review it fairly.",
+    "errorGeneric": "Something went wrong ({error}). Please try again.",
+    "submit": "Submit report",
+    "submitting": "Submitting…",
+    "cancel": "Cancel",
+    "back": "Back",
+    "doneTitle": "Report received",
+    "doneBody": "Thanks — our team will review it. Messages are never deleted, so we can see exactly what happened.",
+    "reasons": {
+      "unresponsive": "Unresponsive",
+      "unavailable": "Apartment unavailable",
+      "inaccurate": "Inaccurate listing",
+      "not_serious": "Not a serious renter",
+      "inappropriate": "Inappropriate or abusive messages",
+      "fraudulent": "Fraudulent / scam",
+      "something_else": "Something else"
+    }
+  },
   "home": {
     "heroEyebrow": "No brokers · Pass your apartment on directly",
     "heroTitle": "Moving soon? Pass your apartment to the next person.",
diff --git a/src/i18n/ru.json b/src/i18n/ru.json
index e098aa1..b525d99 100644
--- a/src/i18n/ru.json
+++ b/src/i18n/ru.json
@@ -62,6 +62,29 @@
     "closedDidntWork": "Разговор закрыт — не сложилось.",
     "closed": "Разговор закрыт."
   },
+  "report": {
+    "title": "Пожаловаться",
+    "subtitle": "Сообщество Peredai построено на доверии и безопасности. Расскажите подробнее — мы разберёмся.",
+    "detailLabel": "Расскажите, что произошло (обязательно)",
+    "detailPlaceholder": "Несколько деталей помогут нашей команде объективно разобраться.",
+    "consent": "Отправляя жалобу, вы соглашаетесь передать эту переписку команде Peredai для объективного рассмотрения.",
+    "errorGeneric": "Что-то пошло не так ({error}). Попробуйте ещё раз.",
+    "submit": "Отправить жалобу",
+    "submitting": "Отправляем…",
+    "cancel": "Отмена",
+    "back": "Назад",
+    "doneTitle": "Жалоба получена",
+    "doneBody": "Спасибо — наша команда рассмотрит её. Сообщения никогда не удаляются, поэтому мы видим, что именно произошло.",
+    "reasons": {
+      "unresponsive": "Не отвечает",
+      "unavailable": "Квартира недоступна",
+      "inaccurate": "Неточное объявление",
+      "not_serious": "Несерьёзный арендатор",
+      "inappropriate": "Неуместные или оскорбительные сообщения",
+      "fraudulent": "Мошенничество / обман",
+      "something_else": "Другое"
+    }
+  },
   "home": {
     "heroEyebrow": "Без посредников · Передача квартиры напрямую",
     "heroTitle": "Скоро переезд? Передайте свою квартиру следующему жильцу.",
